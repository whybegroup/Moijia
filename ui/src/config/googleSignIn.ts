/** Web bundle stub; native uses `googleSignIn.native.ts`. */
export async function signInWithGoogleIdTokenNative(
  _webClientId: string,
): Promise<{ idToken: string; accessToken: string } | null> {
  return null;
}
